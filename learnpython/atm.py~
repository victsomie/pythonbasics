def atm():
    count = 0
    passw = 1234
    """

    if (num == passw):
        count += 1
        print "Logged in at attempt %s " % count
    elif (num != passw) and (count <= 2):
        count += 1
        print "Wrong! Try again!"
        print "Count now is: %s times" % count
        atm()
    elif (num != passw) and (count > 2):
        print "Account is locked"
    

    """
    for i in range(0, 4):
        num = raw_input("Enter your password: ")
        if (num == ''):
            print "You didn't enter anything!"

        if (num == passw):
            count += 1
            print "Awesome at attempt %s " % count
            break
        elif (num != passw):
            if (count<3):
                count += 1
                print "Incorrect %s " % count
            elif (count > 2):
                count += 1
                print "Blocked at attempt %s " % count
                break


atm()
