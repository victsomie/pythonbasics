#!/usr/bin/python

#A simple game to combine different function

times = []
j = int(raw_input("Enter a number"))

time.append(j)


def jump(times):
    if len(times)=5:
        break
    else:
        print "Awesome number"
